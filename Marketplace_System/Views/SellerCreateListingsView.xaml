<UserControl x:Class="Marketplace_System.Views.SellerCreateListingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Border Background="White"
            CornerRadius="18"
            Padding="20"
            BorderBrush="#DCEAE3"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" LastChildFill="False">
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="Create Listings"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="#1F4E3D"/>
                    <TextBlock Text="Manage your products in one place. Create, update, or remove listings anytime."
                               Foreground="#6B7280"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <Button x:Name="CreateListingButton"
                        DockPanel.Dock="Right"
                        Content="+ Create Listing"
                        Click="CreateListingButton_Click"
                        Padding="16,8"
                        Margin="10,0,0,0"
                        Background="#2E7D5B"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        VerticalAlignment="Center"/>
            </DockPanel>

            <Grid Grid.Row="2">
                <TextBlock x:Name="EmptyStateTextBlock"
                           Text="No listings yet. Click ‘Create Listing’ to publish your first product."
                           Foreground="#6B7280"
                           FontStyle="Italic"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="Collapsed"/>

                <DataGrid x:Name="ListingsDataGrid"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          IsReadOnly="True"
                          CanUserResizeRows="False"
                          CanUserAddRows="False"
                          GridLinesVisibility="Horizontal"
                          BorderThickness="1"
                          BorderBrush="#E5E7EB"
                          Background="White"
                          RowBackground="White"
                          AlternatingRowBackground="#F9FDFA"
                          SelectionMode="Single"
                          Margin="0,0,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="2*"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                        <DataGridTextColumn Header="Price / kg" Binding="{Binding PriceText}" Width="*"/>
                        <DataGridTextColumn Header="Stock" Binding="{Binding StockText}" Width="*"/>
                        <DataGridTextColumn Header="Updated" Binding="{Binding UpdatedText}" Width="*"/>
                        <DataGridTemplateColumn Header="Actions" Width="170">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="Edit"
                                                Click="EditButton_Click"
                                                Tag="{Binding Id}"
                                                Padding="8,4"
                                                Margin="0,0,6,0"
                                                Background="#E6F4EC"
                                                BorderThickness="0"
                                                Cursor="Hand"/>
                                        <Button Content="Delete"
                                                Click="DeleteButton_Click"
                                                Tag="{Binding Id}"
                                                Padding="8,4"
                                                Background="#FEE2E2"
                                                BorderThickness="0"
                                                Cursor="Hand"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
    </Border>
</UserControl>
