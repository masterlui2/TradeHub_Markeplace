<UserControl x:Class="Marketplace_System.Views.AdminActivityLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Buyer &amp; Seller Activity Logs" FontSize="18" FontWeight="SemiBold" Foreground="#0F172A" DockPanel.Dock="Left"/>
            <TextBox Width="260" Height="34" Padding="10,4" Text="{Binding ActivitySearchText, UpdateSourceTrigger=PropertyChanged}" DockPanel.Dock="Right"/>
        </DockPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <ComboBox Style="{DynamicResource AdminComboBoxStyle}" Width="120" Margin="0,0,10,0" SelectedValuePath="Content" SelectedValue="{Binding ActivityRoleFilter}">
                <ComboBoxItem Content="All"/>
                <ComboBoxItem Content="Buyer"/>
                <ComboBoxItem Content="Seller"/>
            </ComboBox>
            <DatePicker Width="150" SelectedDate="{Binding ActivityStartDate}" Margin="0,0,10,0"/>
            <DatePicker Width="150" SelectedDate="{Binding ActivityEndDate}"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding ActivityLogSummary}" Margin="0,0,0,8" Foreground="#64748B"/>

        <DataGrid Grid.Row="3" Style="{DynamicResource AdminDataGridStyle}" ItemsSource="{Binding ActivityLogsView}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm}" Width="*"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Action" Binding="{Binding ActionDescription}" Width="3*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>